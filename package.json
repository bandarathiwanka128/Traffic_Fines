{
  "name": "traffic-fines",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "build": "npm run build --workspace=frontend",
    "check:backend": "node scripts/check-backend.js",
    "ci": "npm run check:backend && npm run build"
  },
  "engines": {
    "node": "22.x"
  }
}
